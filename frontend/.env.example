# Backend API Configuration
# Base URL for the backend API (default: http://localhost:3000)
VITE_BACKEND_API_BASE_URL=http://localhost:3000

# Backend API Key (Optional)
# Required if backend authentication is enabled
# Leave empty if backend authentication is disabled (development mode)
VITE_BACKEND_API_KEY=your-backend-api-key-here
